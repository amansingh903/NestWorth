export default function ExperienceCenter({ city }) {
  return (
    <section className="container">
      <h2>Home Interiors in {city}</h2>
      <p>No physical experience center available currently.</p>
    </section>
  );
}
